

how to feed a list to a module. works


  # The word after module. below is the "name" I give in the subnet*.tf file when creating subnets
  # and the last word is what the module explicitly exposes i.e. created_subnet_id
  # works
  subnets = ["${module.subnet_dmz_east_1a.created_subnet_id}","${module.subnet_dmz_east_1b.created_subnet_id}"]



# commands to mount efs

  cd /
  sudo yum install -y amazon-efs-utils
  sudo yum install -y nfs-utils
  sudo mdkir /efs
  sudo mount -t nfs4 -o nfsvers=4.1,rsize=1048576,wsize=1048576,hard,timeo=600,retrans=2 fs-b5e3821c.efs.us-west-2.amazonaws.com:/ /efs


